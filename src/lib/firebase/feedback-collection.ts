/**
 * Single source of truth for the feedback Firestore collection name.
 * Imported by both server (admin SDK) and client (web SDK) callers.
 */
export const FEEDBACK_COLLECTION = 'feedback_items';
